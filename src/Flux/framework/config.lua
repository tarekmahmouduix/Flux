local config = {
    _version = "0.1.0@beta",
    _build = "20240910",
    defaultSettings = {
        theme = "light", -- beta feature[locked]
        debugMode = false,
        logs = false,
    },
}

return config